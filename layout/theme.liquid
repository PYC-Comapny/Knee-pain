<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    {{ content_for_header }}
    <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
    <title>Shopify Theme</title>
  </head>
  <body>
    {{ content_for_layout }}
  </body>
 </html>
